% meow/1

def i32 fib() -> i32 {
  when {
    self === 0n => 0n;
    self === 1n => 1n;
    otherwise => (self - 1n) fib() + (self - 2n) fib();
  }
}

def main(_: array) {
  transcript log(30n fib());
}