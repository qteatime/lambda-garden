{
  "name": "@qteatime/meow-compiler",
  "version": "0.0.1",
  "description": "A lower-level computation language based on functional programming with effect handlers and multi-methods.",
  "main": "build/index.js",
  "bin": {
    "meow": "bin/meow.js",
    "meowc": "bin/meowc.js",
    "imeow": "bin/imeow.js"
  },
  "directories": {
    "example": "examples",
    "test": "test"
  },
  "scripts": {
    "test": "npm run build && node --test && meow --test --no-stdlib test/packages.meow",
    "compile": "tsc -p .",
    "build-prelude": "npm run compile && node support/make-prelude.js",
    "build": "npm run compile && npm run build-prelude",
    "build-core": "meowc --no-prelude --no-test --package meow.core lib/core.meow > lib/_core.js",
    "build-grammar": "node ../tools/lingua.js grammar.lingua typescript > source/generated/grammar.ts",
    "build-all": "npm run build && npm run build-core"
  },
  "author": "Niini",
  "license": "MPL-2.0",
  "devDependencies": {
    "@types/node": "^20.10.7",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "ohm-js": "^15.5.0"
  }
}
